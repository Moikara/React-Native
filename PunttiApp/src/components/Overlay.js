import React, { useState } from 'react';
import { View, Button, Modal } from 'react-native';

export default Overlay = ({ show, dispatch }) => {
    const [input, setInput] = useState("");
    const inputRef = useRef(null);

    return (
        <Modal>
            <View>
                <View>

                </View>
            </View>
        </Modal>
    )
}

